namespace Core.WhiteLabel;

/// <summary>
/// Groups white-label options into the tabs the Owner "Deployment" settings surface renders. Each value
/// maps to one tab; the ordering here is the tab order.
/// </summary>
public enum WhiteLabelCategory
{
    Branding,
    Theme,
    Features,
    Registration,
    Accounts,
    Email,
    Ai,
    OpenApi,
    PropFirm
}
