@page "/optimize"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Policy = "UserOrAbove")]

<PageTitle>Optimize</PageTitle>

<ComingSoon Title="Optimization is coming soon"
            Icon="@Icons.Material.Filled.Tune"
            Description="Sweep a cBot's parameters across ranges, backtest every combination on your nodes, and rank the results to find the strongest settings. We're putting the finishing touches on it." />
